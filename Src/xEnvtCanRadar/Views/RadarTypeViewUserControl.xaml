<UserControl x:Class="xEnvtCanRadar.Views.RadarTypeViewUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
             xmlns:Views="clr-namespace:xEnvtCanRadar" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="OnLoaded">
  <Grid>
    <Grid.Resources>
      <DataTemplate x:Key="gif">
        <Views:LbxItemRadarTemplate Margin="0" Height="12" Width="19" />
      </DataTemplate>
    </Grid.Resources>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="1*"/>
      <RowDefinition Height="1*"/>
    </Grid.RowDefinitions>

    <TextBlock x:Name="tbxTitle" d:Text="Found CCC thingies." VerticalAlignment="Top" HorizontalAlignment="Center" Foreground="#08f"/>

    <Views:LbxItemRadarTemplate Grid.Row="1" DataContext="{Binding ElementName=lbx, Path=SelectedItem}" Height="480" Width="580" />
    
    <ListView Grid.Row="2" x:Name="lbx" ItemTemplate="{StaticResource gif}" Background="#222" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
      <ListView.ItemsPanel>
        <ItemsPanelTemplate>
          <WrapPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>
      </ListView.ItemsPanel>
    </ListView>

  </Grid>
</UserControl>
